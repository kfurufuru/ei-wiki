---
title: "計装基礎"
description: "4-20mA・HART・フィールドバス基礎"
tags:
  - 計装
audience:
  - 電気担当
last_verified: 2026-08-05
---

# 計装基礎

## 30秒まとめ

4-20mAは「電流ループ」で信号伝送する業界標準。4mAがゼロスケール（断線と区別できる）、20mAがフルスケール。電圧伝送より耐ノイズ性が高く、長距離伝送に向く。HARTはこのアナログ信号にデジタル通信を重畳する技術。

---

## 4-20mAループの仕組み

### なぜ4mAがゼロか

| 電流値 | 区分 | 意味 |
|--------|------|------|
| 0 mA | — | 断線・電源失 |
| ≤ 3.6 mA | 故障判定（NAMUR NE43） | 断線・センサ異常（フェイルロー方向） |
| 3.8 mA | 有効測定範囲 下限 | これ未満はアンダーレンジ |
| 4 mA | ゼロスケール | 正常な最小信号 |
| 20 mA | フルスケール | 正常な最大信号 |
| 20.5 mA | 有効測定範囲 上限 | これ超はオーバーレンジ |
| ≥ 21 mA | 故障判定（NAMUR NE43） | センサ異常（フェイルハイ方向） |

!!! note "「有効測定範囲」と「故障判定閾値」は別物"
    - **有効測定範囲 3.8〜20.5 mA**: メーカが直線性を保証する計測有効範囲。この外はアンダー／オーバーレンジ。
    - **故障判定 ≤3.6 mA ／ ≥21 mA（NAMUR NE43）**: 断線・センサ異常を検出するフェイル判定の閾値。
    - 3.6〜3.8 mA・20.5〜21 mA は遷移域（通常は使用しない領域）。
    数字を混同しやすいので、「計測の有効範囲」か「故障検出の閾値」かを区別して扱います。

<figure>
<svg viewBox="0 0 560 412" width="560" height="412" role="img"
     aria-labelledby="fig-420ma-bands-title" xmlns="http://www.w3.org/2000/svg">
  <title id="fig-420ma-bands-title">4-20mA信号の電流帯の区分。下から、3.6mA以下は故障判定（フェイルロー方向）、3.6から3.8mAは遷移域、3.8から20.5mAが有効測定範囲でそのうち4から20mAが正常な信号範囲、20.5から21mAは遷移域、21mA以上は故障判定（フェイルハイ方向）。0mAは断線・電源失。</title>
  <!-- 電流軸 -->
  <g style="stroke: var(--md-default-fg-color); fill: none" stroke-width="1.4">
    <line x1="60" y1="40" x2="60" y2="388"/>
    <line x1="54" y1="88" x2="60" y2="88"/>
    <line x1="54" y1="124" x2="60" y2="124"/>
    <line x1="54" y1="160" x2="60" y2="160"/>
    <line x1="54" y1="268" x2="60" y2="268"/>
    <line x1="54" y1="304" x2="60" y2="304"/>
    <line x1="54" y1="340" x2="60" y2="340"/>
    <line x1="54" y1="388" x2="60" y2="388"/>
  </g>
  <!-- 軸の省略記号（4〜20 mA の区間を圧縮している印） -->
  <g style="stroke: var(--md-default-fg-color); fill: none" stroke-width="1.2">
    <line x1="53" y1="222" x2="67" y2="210"/>
    <line x1="53" y1="230" x2="67" y2="218"/>
  </g>
  <!-- 有効測定範囲のブラケット -->
  <g style="stroke: var(--md-default-fg-color); fill: none" stroke-width="1.4">
    <line x1="138" y1="124" x2="138" y2="304"/>
    <line x1="138" y1="124" x2="146" y2="124"/>
    <line x1="138" y1="304" x2="146" y2="304"/>
  </g>
  <!-- 帯 -->
  <g style="stroke: var(--ei-fig-danger); fill: none" stroke-width="1.6">
    <rect x="150" y="40" width="230" height="48"/>
    <rect x="150" y="340" width="230" height="48"/>
  </g>
  <g style="stroke: var(--ei-fig-warn); fill: none" stroke-width="1.6">
    <rect x="150" y="88" width="230" height="36"/>
    <rect x="150" y="304" width="230" height="36"/>
  </g>
  <g style="stroke: var(--md-default-fg-color); fill: none" stroke-width="1.6">
    <rect x="150" y="124" width="230" height="180"/>
    <rect x="168" y="160" width="194" height="108" rx="4"/>
  </g>
  <!-- 軸の値 -->
  <g style="fill: var(--md-default-fg-color)" font-size="11">
    <text x="48" y="92" text-anchor="end">21</text>
    <text x="48" y="128" text-anchor="end">20.5</text>
    <text x="48" y="164" text-anchor="end">20</text>
    <text x="48" y="272" text-anchor="end">4</text>
    <text x="48" y="308" text-anchor="end">3.8</text>
    <text x="48" y="344" text-anchor="end">3.6</text>
    <text x="48" y="392" text-anchor="end">0</text>
    <text x="26" y="214" text-anchor="middle" transform="rotate(-90 26 214)">電流 [mA]</text>
  </g>
  <!-- 有効測定範囲のラベル -->
  <g style="fill: var(--md-default-fg-color)" font-size="11">
    <text x="120" y="214" text-anchor="middle" transform="rotate(-90 120 214)">有効測定範囲（直線性を保証）</text>
  </g>
  <!-- 帯の中の文字 -->
  <g font-size="11">
    <text x="265" y="60" text-anchor="middle" style="fill: var(--ei-fig-danger)">故障判定（NAMUR NE43）</text>
    <text x="265" y="78" text-anchor="middle" style="fill: var(--ei-fig-danger)">フェイルハイ方向</text>
    <text x="265" y="110" text-anchor="middle" style="fill: var(--ei-fig-warn)">遷移域（通常は使用しない）</text>
    <text x="265" y="326" text-anchor="middle" style="fill: var(--ei-fig-warn)">遷移域（通常は使用しない）</text>
    <text x="265" y="360" text-anchor="middle" style="fill: var(--ei-fig-danger)">故障判定（NAMUR NE43）</text>
    <text x="265" y="378" text-anchor="middle" style="fill: var(--ei-fig-danger)">フェイルロー方向</text>
  </g>
  <g style="fill: var(--md-default-fg-color)" font-size="12">
    <text x="265" y="198" text-anchor="middle">正常な信号範囲</text>
  </g>
  <g style="fill: var(--md-default-fg-color--light)" font-size="10">
    <text x="265" y="220" text-anchor="middle">4 mA ＝ ゼロスケール</text>
    <text x="265" y="238" text-anchor="middle">20 mA ＝ フルスケール</text>
  </g>
  <!-- 右側の注記 -->
  <g style="fill: var(--md-default-fg-color--light)" font-size="11">
    <text x="394" y="68" text-anchor="start">センサ異常</text>
    <text x="394" y="368" text-anchor="start">断線・センサ異常</text>
    <text x="394" y="392" text-anchor="start">0 mA は断線・電源失</text>
  </g>
</svg>
<figcaption>「計測の有効範囲（3.8〜20.5 mA）」と「故障検出の閾値（3.6 mA 以下／21 mA 以上）」は別物で、その間が遷移域。縦軸は 4〜20 mA の区間を圧縮しており等間隔ではない（軸の二重斜線が省略の印）。</figcaption>
</figure>

0mAを「生きている信号」として使わないことで、**断線と測定値ゼロを区別できる**。これが4-20mAの最大の利点。現場でアラームが出たとき、まず「0mAか否か」を確認する習慣をつける。

### ループの電流計算

```
受信計器の表示値 = (I - 4mA) / 16mA × スパン + ゼロ点
```

例：0〜100℃ スパン、受信電流12mA の場合
```
(12 - 4) / 16 × 100 = 50℃
```

---

## 電流伝送 vs 電圧伝送

| 項目 | 電流伝送 (4-20mA) | 電圧伝送 (1-5V等) |
|------|------------------|------------------|
| 耐ノイズ性 | 高い | 低い |
| 長距離伝送 | 可（数百m） | 不向き（電圧降下） |
| 配線抵抗の影響 | 受けにくい | 受ける |
| 主な用途 | フィールド〜DCS間 | 盤内短距離 |

電流伝送が耐ノイズ性に優れる理由：ノイズは電圧として乗ってくるが、電流ループでは受信側の入力抵抗両端の電圧誤差にしかならず、電流自体は変化しない。

!!! warning "配線抵抗の上限"
    ループ電源電圧（通常24V）から伝送器の最低動作電圧（約12V）を引いた残りが許容電圧降下。
    許容負荷抵抗 = (24V - 12V) / 0.020A = **600Ω** が一般的な上限目安。
    受信計器の入力抵抗＋ケーブル抵抗の合計がこれを超えないこと。

---

## HART通信の概要

HART（Highway Addressable Remote Transducer）は、4-20mAアナログ信号に**1200bpsのデジタル信号をFSK変調で重畳**する通信方式。

```
アナログ信号  4mA ────────────────────── 20mA
デジタル信号  ~~~~波形（1200Hz/2200Hz）~~~~
             同一のケーブル上に共存
```

FSK（Frequency Shift Keying）で、論理「1」を1200Hz・論理「0」を2200Hzの正弦波に割り当て、平均値がゼロになる交流波（振幅 ±0.5 mA）としてアナログ電流に重畳します。平均電流が変化しないため、4-20mAのアナログ計測値を乱さずにデジタル通信を同時に流せます。

### HARTでできること

- 機器情報の読み出し（製造番号・タグ・ステータス）
- 設定変更（レンジ・単位・減衰）
- 診断情報の取得（自己診断結果）
- 複数パラメータの同時取得（動的変数 PV／SV／TV／QV の4つ）

!!! tip "現場での活用"
    ループキャリブレータ（FLUKE 707 等）にHARTモデムを繋げると、フィールドで伝送器のゼロ・スパン調整や診断ができる。工事・保全作業の効率が大きく上がる。

### ループ抵抗の要件（重畳通信の成立条件）

HARTのFSK信号は電流変調です。マスタ（ハンドヘルドや変調器）が読み取るには、ループのどこかに抵抗があって電流変動を電圧信号に変換する必要があります。

- **標準 250 Ω**: 4mA→1V・20mA→5V に変換でき、DCSのアナログ入力の受信抵抗としても一般的。
- **下限 230 Ω**: これを下回るとFSK信号の振幅が不足し、通信が不安定になります。
- **上限の目安 約 600 Ω**: HART仕様が推奨する範囲は 230〜600 Ω。これ以上はループ電源電圧との兼ね合い（前述「電流伝送 vs 電圧伝送」の配線抵抗の上限で述べた電圧降下）で伝送器が動かなくなります。

!!! warning "HART通信できないときの定番原因"
    受信抵抗が 230 Ω 未満、あるいは通信機の接続位置に抵抗が挟まっていないケースが多いです。DCS入力抵抗が低い盤では、伝送器端子に外付け 250 Ω を入れて通信することがあります。診断手順は [信号系トラブル](../06-trouble/signal.md) を参照してください。

### マルチドロップ接続

1つのループに複数のHART機器（伝送器）をぶら下げる接続方式です。

- 各機器の**アナログ電流を 4 mA に固定**し、プロセス値はデジタル通信だけで読み出します（アナログ計測は使えません）。
- 機器はポーリングアドレスで識別します。**HART 5 まではアドレス 1〜15（最大15台）**、**HART 6/7 では拡張アドレスで最大63台**まで対応します。
- マスタが各機器を順番にポーリングするため、**更新周期が遅くなる**（1機器あたり数百ms〜秒オーダ）のがトレードオフです。応答速度が要る制御ループには不向きで、監視用の多点計測（タンクヤードの温度・液位など）に向きます。

### バーストモード

通常のHARTはマスタが要求してスレーブが応答する「要求応答」型ですが、バーストモードでは伝送器が要求を待たずに一定周期でデータを送出し続けます。ポーリングのオーバーヘッドがなくなり、更新周期を上げられます。WirelessHARTの定期送信もこの考え方を使います。

### WirelessHART（IEC 62591）

既設の4-20mA/HART配線を引き直さずに、無線でHARTデータを収集する方式です。

- 各フィールド機器が**自己組織化メッシュ**を形成し、機器同士が中継してゲートウェイまでデータを届けます。障害物や1ノード故障があっても経路を張り替えます。
- 既設の2線式伝送器には**WirelessHARTアダプタを後付け**でき、配線工事なしで診断情報を吸い上げられます。
- 用途は監視・診断・非制御の補助計測が中心で、応答性やSIL要求の高い制御ループには使いません。

### HART値の一括読み出し

多数のHART機器の診断情報を活用するには、盤側でデジタル値を取り込む仕組みが要ります。

- **HART対応I/Oカード**: DCS/PLCのアナログ入力カード自体がHARTを復調し、アナログ値とデジタル診断を同時に取り込みます。新設プラントの標準的な構成です。
- **HARTマルチプレクサ**: 既設の非HART入力カードを活かしたまま、端子台に多重化装置を追加してHART値を別系統（保全用アセットマネジメントなど）へ吸い上げます。既設改造で使われます。

---

## 2線式と4線式

### 2線式（ループパワード）

```
[DCS/バリア] ─── (+) ─── [伝送器] ─── (-) ─── [DCS/バリア]
              24VDCを供給         信号電流(4-20mA)を返す
```

- 電源と信号を**同一の2本のケーブル**で賄う
- 伝送器の消費電力は最大4mA×最低動作電圧で制限される
- 配線コストが安い → 現場では主流

### 4線式（セパレートパワード）

```
信号線  ─ (+) / (-) ─  伝送器
電源線  ─ (+) / (-) ─  伝送器（別電源 100/200VAC または 24VDC）
```

- 分析計・ガス検知器・超音波流量計など**消費電力の大きい機器**に使用
- 電源と信号が独立しているため、電源電圧の変動が信号に影響しない

---

## フィールドバスのセグメント設計

4-20mA/HARTが「1機器＝1ループ」なのに対し、**フィールドバス（Fieldbus）は1本のケーブル（セグメント）に複数のディジタル機器を接続**します。化学プラントで使う代表は **FOUNDATION Fieldbus H1** と **PROFIBUS PA** で、どちらも **IEC 61158-2 の同じ物理層（31.25 kbps・MBP：Manchester Bus Powered、2線でバス給電）** を共有します。規格の位置づけは [計装工事で押さえるJIS規格](jis-standards.md) の「B. 信号」を参照してください。

### トランクとスパーの構造

```text
[電源コンディショナ]─┬─ トランク（幹線）───────────────┬─[終端抵抗]
   [H1 I/Oカード]    │                                  │
                     ├─スパー─[機器]   ├─スパー─[機器] │
                     │        (支線)   │               │
                   [終端抵抗]        ジャンクションボックス
```

- **トランク（幹線）**: セグメントの背骨。両端に終端抵抗を1個ずつ置きます。
- **スパー（支線）**: トランクから各機器へ分岐する短い枝線。ジャンクションボックスやデバイスカプラで分けます。

### 終端抵抗は「両端に2個」

- セグメントには**終端抵抗をちょうど2個**（トランクの両端に1個ずつ）入れます。中身は 100 Ω 抵抗と 1 μF コンデンサの直列で、信号の反射を防ぎます。
- **1個不足**（1個だけ）だと反射で波形が乱れ通信エラー、**3個以上**（過剰）だと信号振幅が下がって同じく通信不良になります。増設・改造で電源コンディショナ内蔵の終端を二重に入れてしまう事故が定番です。

### 電力バジェット（給電設計の要）

バス給電（2線で電源と通信を兼ねる）のため、**全機器の消費電流とケーブル電圧降下を積み上げ、セグメント末端の機器電圧が最低動作電圧を割らない**ことを設計時に確認します。

- FF H1 機器の**最低動作電圧は 9 V DC**。電源コンディショナ出力（トランク始点で約 24〜32 V）から、全機器の消費電流 × ケーブル抵抗で生じる電圧降下を引いた末端電圧が 9 V 以上必要です。
- 機器1台の消費電流は**おおむね 10〜20 mA**（機器仕様値）。設計では合計電流に**約10%の余裕**を見込みます。
- 冗長化のため電源コンディショナは二重化して設置するのが一般的です。

### セグメント長・スパー長・機器台数の制約

| 項目 | 目安（Type A ケーブル・非本安） | 備考 |
|------|------------------------------|------|
| バス速度 | 31.25 kbps | FF H1／PROFIBUS PA 共通 |
| **総延長（トランク＋全スパーの合計）** | 1900 m | **トランク単独の上限ではありません**。ケーブル種別で変わる |
| トランク単独の長さ | 1200 m 以下とする設計指針あり | 総延長 1900 m とは別枠の目安 |
| スパー長 | 接続台数で変わる（1台なら 120 m、台数が増えるほど短縮） | 台数区分ごとの値は資料により差がある（下記「根拠」参照） |
| 1セグメント機器台数 | 4〜16台程度 | 電力バジェットと実行周期で決まる。非本安で最大16台程度 |
| 終端抵抗 | 2個（両端） | 過不足厳禁 |

!!! warning "1900 m は「トランクの長さ」ではない"
    1900 m は**トランクと全スパーを足した総延長**です。トランク単独の上限と読むと、スパーの合計分だけ設計が長側に外れます（本ページは 2026-08-05 に「トランク最大長 1900 m」から是正しました）。

!!! note "台数は「電力」と「速度」の二重制約"
    電力バジェット上は載っても、機器が増えるとマクロサイクル（全機器を巡回する制御周期）が延びます。速い制御が要るループは台数を絞るか、4-20mAを併用します。

### 本安（防爆）との関係 — FISCO/FNICO

危険区域でフィールドバスを本質安全（Ex i）にする設計コンセプトが **FISCO（Fieldbus Intrinsically Safe Concept）** と **FNICO（非点火：Zone 2向け）** です。

- FISCOでは機器・ケーブル・電源のエンティティパラメータを規格枠内に収めることで、個別の本安計算を簡略化します。
- 本安にすると給電電力が絞られるため**台数と距離はさらに制限**されます。FISCO では**セグメント長 1000 m・スパー長 30 m** が上限として広く示されています（非本安の総延長 1900 m・スパー 120 m から、それぞれ縮む）。台数は電力バジェットで数台程度に絞られます。
- 本安の基本概念・エンティティパラメータは [防爆](explosion-proof.md) を参照してください。

---

## 4-20mA / HART / フィールドバスの使い分け

| 観点 | 4-20mA（アナログ） | HART（重畳） | フィールドバス（H1/PA） |
|------|------------------|-------------|----------------------|
| 配線 | 1機器1ループ | 1機器1ループ（既設流用可） | 1セグメントに複数台 |
| 診断情報 | なし（電流のみ） | あり（アナログ優先＋診断） | 豊富（全ディジタル） |
| 制御応答 | 速い | 速い（アナログは即時） | セグメント台数依存でやや遅い |
| 配線量・コスト | 点数比例で増える | 4-20mAと同等 | ケーブル本数を大きく削減 |
| 既設流用 | — | そのまま診断追加可（後付けHART/無線） | 基本は新設・専用設計 |
| 向く用途 | 単純ループ・安全計装 | 既設の診断強化・多点監視 | 新設大規模プラントの機器集約 |

- **既設プラントの診断強化**なら、配線を変えずに済むHART（対応I/O・マルチプレクサ・WirelessHART）が第一候補です。
- **新設で機器点数が多い**なら、ケーブルを削減できるフィールドバスが効きます。ただし終端・電力・台数のセグメント設計が前提です。
- **安全計装（SIS）や高速・単純なループ**は、シンプルで独立性の高い4-20mAを選ぶのが基本です（SIL設計は [安全計装 SIS/SIL](sis-sil.md) 参照）。

---

## P&ID 主要記号一覧

| 記号 | 意味 | 例 |
|------|------|----|
| FIC | Flow Indicating Controller（流量指示調節計） | FIC-101 |
| TIC | Temperature Indicating Controller（温度指示調節計） | TIC-201 |
| PIC | Pressure Indicating Controller（圧力指示調節計） | PIC-301 |
| LIC | Level Indicating Controller（液位指示調節計） | LIC-401 |
| FT | Flow Transmitter（流量伝送器） | FT-101 |
| TT | Temperature Transmitter（温度伝送器） | TT-201 |
| PT | Pressure Transmitter（圧力伝送器） | PT-301 |
| LT | Level Transmitter（液位伝送器） | LT-401 |
| AI | Analog Input（アナログ入力） | — |
| AO | Analog Output（アナログ出力） | — |
| FCV | Flow Control Valve（流量調節弁） | FCV-101 |
| HV | Hand Valve（手動弁） | — |

### ISA-5.1 図記号の主要なもの

**外形は「機器の種類」、中の線は「設置場所」で、別々の軸**です。混同しやすいので分けて覚えます。

| 外形 | 意味（機器の種類） |
|---------|------|
| 円 | 単独の計器（ディスクリート機器） |
| 四角の中に円 | 共有表示・共有制御（DCS など） |
| 六角形 | コンピュータ機能 |
| 四角の中にひし形 | PLC（プログラマブル論理制御） |

| 外形の中の横線 | 意味（設置場所） |
|---------|------|
| 線なし | フィールド設置 |
| 実線1本 | 主要位置（中央制御室など）に設置・通常はアクセス可 |
| 二重線 | 補助位置（現場盤・計器ラックなど） |
| 破線 | アクセス不可（盤裏など） |

| 線の種類 | 意味（信号・接続） |
|---------|------|
| 細い実線 | 計器のプロセス接続（導圧管など） |
| 太い実線 | 配管（プロセス配管） |
| 斜線を重ねた線 | 空気信号（空気配管） |
| 破線 | 電気信号（4-20mA など） |
| 丸を並べた線 | データリンク（フィールドバス・通信） |

!!! warning "旧記載からの是正"
    本ページはかつて「四角＝盤面設置計器」「六角（◇）＝コンピュータ機能」「上部に線のある円＝盤裏設置」「実線＝空気配管」「二重線＝フィールドバス」と書いていました。いずれも**外形（機器種別）と設置場所と信号線種を1つの表に混ぜたことによる取り違え**で、2026-08-05 に上の3表へ分けて是正しました。特に、
    **実線は空気信号ではなく**（空気信号は斜線入りの線）、**破線は「盤裏」ではなく電気信号／アクセス不可**を表し、**二重線はフィールドバスではなく補助位置**です。
    図面判読は最終的に**その図面の凡例**に従ってください（社内標準で独自化されていることがあります）。

---

## よくある現場トラブル

!!! failure "断線との区別"
    DCSのトレンドで突然0%（4mA相当）になったとき、プロセス値が本当にゼロになったのか断線かを確認する。ループチェッカーで実電流を測定する。

!!! failure "アース（シールド）問題"
    シールドの多点接地によるグラウンドループで、トレンドにノイズが乗ることがある。シールドは片端接地（DCS側）が原則。

!!! failure "負荷抵抗オーバー"
    安全バリア（本安バリア）を後付けする際、ループ抵抗が増えて伝送器が正常動作しないことがある。増設前にループ抵抗計算を確認する。

---

## 根拠

### 本ページが正典の範囲

4-20mA ループの成立条件（電流帯の区分・負荷抵抗）、HART の重畳原理とループ抵抗要件、フィールドバスのセグメント設計は**本ページが正典**です。信号系トラブルの切り分け手順は [信号系トラブル](../06-trouble/signal.md)、防爆の本安概念は [防爆](explosion-proof.md)、規格の位置づけは [計装工事で押さえるJIS規格](jis-standards.md) が正典で、本ページは数値を再記述しません。

### 一次計算で検算済み（規格を必要としない）

| 項目 | 検算 |
|---|---|
| 受信計器の表示値 | (12 − 4)/16 × 100 = 50 ℃（0〜100 ℃ スパン・12 mA） |
| 許容負荷抵抗 | (24 − 12) V ÷ 0.020 A = 600 Ω。**分母は最大電流 20 mA**（4 mA で計算すると 3 倍過大になる） |
| 250 Ω での電圧変換 | 4 mA × 250 Ω = 1 V、20 mA × 250 Ω = 5 V |

### 是正の記録（2026-08-05）

- **フィールドバスの 1900 m をトランク単独の上限と書いていた**: 1900 m は**トランク＋全スパーの総延長**です。トランク単独と読むとスパー合計の分だけ設計が長側（危険側）に外れます。あわせて、トランク単独は 1200 m 以下とする設計指針を別枠で示しました
- **本安（FISCO）のスパー長を 60 m と書いていた**: FISCO ではセグメント 1000 m・**スパー 30 m** が上限として広く示されています。60 m は非本安側の台数依存の値と取り違えたものです
- **「IIC で幹線約 1 km・IIB/IIA で最大 1.9 km」という記述を撤回**: FISCO の 1000 m 制限をガスグループ別に読み替える根拠を確認できませんでした
- **スパー最大長 60 m を無条件の上限として書いていた**: スパー長は接続台数で変わります（1台なら 120 m）。台数区分ごとの値は資料により差があるため、本ページは特定の区分表を載せません
- **ISA-5.1 の記号表が3つの軸を混ぜていた**: 「四角＝盤面設置」「上部に線のある円＝盤裏」「実線＝空気配管」「二重線＝フィールドバス」を、**外形（機器種別）／中の線（設置場所）／線種（信号）**の3表に分けて是正しました
- **HART の「最大4変数」**: 動的変数 PV／SV／TV／QV の4つであることを明示しました（機器変数の一括読み出し数は版によって異なるため、数を断定しません）

### 二次資料で一致を確認した数値（原本未照合）

| 項目 | 値 | 出所の状態 |
|---|---|---|
| NAMUR NE43 の故障判定閾値 | ≤3.6 mA／≥21.0 mA、有効測定範囲 3.8〜20.5 mA | NE43 本文は未入手。複数の計装メーカー資料で一致 |
| HART の物理層 | 1200 bps・FSK（論理1 = 1200 Hz／論理0 = 2200 Hz）・振幅 ±0.5 mA | HART 仕様書は未入手 |
| HART のループ抵抗 | 標準 250 Ω・下限 230 Ω・上限の目安 600 Ω | 同上 |
| FF H1／PROFIBUS PA の物理層 | 31.25 kbps・IEC 61158-2（MBP） | IEC 61158-2 は未入手 |
| FF H1 機器の最低動作電圧 | 9 V DC | 同上 |
| 終端抵抗 | 100 Ω ＋ 1 μF の直列を両端に各1個 | 同上 |
| 非本安の総延長／スパー | 総延長 1900 m（トランク＋全スパー）・スパーは台数依存で最大 120 m | FF の公開設計資料で一致 |
| FISCO | セグメント 1000 m・スパー 30 m | IEC 60079-11（FISCO）本文は未入手 |

### 原本未照合（honest-hold）

- **NAMUR NE43、HART 仕様書（FieldComm Group）、IEC 61158-2、IEC 62591（WirelessHART）、IEC 60079-11、ANSI/ISA-5.1 のいずれも原本を照合していません。** 有償・入手制約があるため、本ページは条項番号・表番号を引用しません
- **ISA-5.1 は 2009 年版と 2024 年版で表題・記号の扱いが変わっています。** 本ページの記述は公開されている解説資料で一致した内容にとどめており、**版を特定した引用ではありません**
- **マルチドロップの台数（HART 5 で15台／HART 6-7 で63台）**、バーストモードの周期、HART 対応 I/O・マルチプレクサの構成も仕様書未照合です
- **フィールドバスの台数区分ごとのスパー長表**は資料間で差があり、確定できていません

### 実務慣行（規格の裏付けなし）

- 伝送器の最低動作電圧を「約 12 V」として 600 Ω を上限目安にする計算（実際の最低動作電圧は機器仕様値で、10.5〜12 V 程度と機種差がある）
- DCS 入力抵抗が低い盤で、伝送器端子に外付け 250 Ω を入れて HART 通信を成立させる手当て
- シールドは片端接地（DCS 側）とする原則（詳細は [接地・ノイズトラブル](../06-trouble/grounding-noise.md)）
- 電源コンディショナの二重化、電力バジェットに約10%の余裕を見込む設計

### 限界の明示

- **本ページの数値だけでセグメント設計を確定しないでください。** 総延長・スパー長・台数は、ケーブル種別・電力バジェット・マクロサイクルの3つで同時に決まります。実設計はメーカーのセグメント計算ツールと機器仕様値によってください
- **P&ID 記号は図面の凡例が優先**です。本ページの表は判読の入口で、社内標準による独自化を上書きしません
- **アンダー／オーバーレンジと故障判定は別物**です（本文の note のとおり）。DCS 側のアラーム設定がどちらの閾値で組まれているかは、盤ごとに確認してください

### 正典参照

| 項目 | 正典 |
|---|---|
| 信号系トラブルの切り分け（HART 不通・ノイズ） | [信号系トラブル](../06-trouble/signal.md) |
| 本安の基本概念・エンティティパラメータ | [防爆](explosion-proof.md) |
| 規格の位置づけ（B. 信号） | [計装工事で押さえるJIS規格](jis-standards.md) |
| SIL 設計・安全計装 | [安全計装 SIS/SIL](sis-sil.md) |
| シールド接地・グラウンドループ | [接地・ノイズトラブル](../06-trouble/grounding-noise.md) |
| ループキャリブレータの機能区分（SOURCE/SINK ほか） | [計測器](../05-hozen/instruments.md) |

照合日: 2026-08-05（計算は本ページで検算。規格原本はいずれも未照合）。
